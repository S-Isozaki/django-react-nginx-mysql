apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: node-modules-volume
  name: node-modules-volume
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
